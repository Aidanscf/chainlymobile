import React from "react";

// Route file: keep routing stable, while the real screen lives in /src/screens
import SavedPresetsScreen from "@/screens/ai/SuspensionHelper/SavedPresets";

export default function AISuspensionPresetsScreen() {
  return <SavedPresetsScreen />;
}
